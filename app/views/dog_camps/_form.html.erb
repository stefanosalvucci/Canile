<%= form_for(@dog_camp) do |f| %>
  <% if @dog_camp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dog_camp.errors.count, "error") %> prohibited this dog_camp from being saved:</h2>

      <ul>
      <% @dog_camp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.number_field :capacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
